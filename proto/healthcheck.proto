syntax = "proto3";

package healthcheck;

message Ping {
    optional string message = 1;
}

message Pong {
    optional string message = 1;
}

service HealthCheck {
    rpc Poll(Ping) returns (Pong);
}
